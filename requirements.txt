# Install torch/torchvision matched to your CUDA version — see pytorch.org.
# Example for CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch
torchvision



# TensorRT: install via NVIDIA (pip package `tensorrt`, or use the
# torch-tensorrt integration). Version must match your CUDA/driver.
# tensorrt
# torch-tensorrt
kaggle==2.2.3
matplotlib==3.11.1
numpy==2.4.6
onnx==1.22.0
onnxruntime-gpu==1.27.0
pillow==12.3.0